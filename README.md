Palindrom-asm

The "Palindrom-asm" program is written in C, with its main function implemented in assembly language.
The program returns a pointer to a structure that contains all the palindromes found in the given string.
You can change the given string in the main funcion 😀
